// Providers — separate ticket. Placeholder so the shell route resolves.
export default function ProvidersPage() {
  return (
    <div className="text-muted-foreground text-sm">Providers — coming soon.</div>
  );
}
