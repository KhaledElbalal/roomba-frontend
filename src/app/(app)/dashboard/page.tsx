// Telemetry — separate ticket. Placeholder so the shell route resolves.
export default function TelemetryPage() {
  return (
    <div className="text-muted-foreground text-sm">Telemetry — coming soon.</div>
  );
}
