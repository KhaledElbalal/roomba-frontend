// New Run — separate ticket. Placeholder so the shell route resolves.
export default function NewRunPage() {
  return (
    <div className="text-muted-foreground text-sm">New run — coming soon.</div>
  );
}
