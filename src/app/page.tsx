import { LandingPage } from "@/components/landing/landing-page";

// Public marketing landing (no app shell). Self-contained, terminal-flavored —
// see src/components/landing/. ROO-27.
export default function Home() {
  return <LandingPage />;
}
